<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WraithLite.ViewModels"
             x:Class="WraithLite.MainPage"
             x:DataType="vm:MainViewModel">

    <!--<VerticalStackLayout Padding="10">
        <ScrollView HeightRequest="400">
            <Label Text="{Binding Output}" 
                   FontFamily="Consolas"
                   FontSize="12"
                   LineBreakMode="WordWrap" />
        </ScrollView>
        <Entry Placeholder="Enter command..." 
               Text="{Binding InputText}" 
               Completed="SendCommand" />
    </VerticalStackLayout>-->
    <VerticalStackLayout Padding="10">
        <Button Text="Connect" Clicked="OnConnectClicked" />
        <Editor x:Name="GameOutput" FontFamily="Consolas" AutoSize="TextChanges" IsReadOnly="True" />
        <Entry x:Name="CommandEntry" Placeholder="Type command..." Completed="OnCommandEntered"/>
    </VerticalStackLayout>
    
</ContentPage>
